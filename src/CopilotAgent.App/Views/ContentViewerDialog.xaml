<Window x:Class="CopilotAgent.App.Views.ContentViewerDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:emoji="clr-namespace:Emoji.Wpf;assembly=Emoji.Wpf"
        mc:Ignorable="d"
        Title="{Binding Title}" 
        Height="550" Width="650"
        MinHeight="400" MinWidth="500"
        WindowStartupLocation="CenterOwner"
        Background="#F5F5F5"
        ResizeMode="CanResizeWithGrip">
    
    <Window.Resources>
        <!-- Card style -->
        <Style x:Key="InfoCard" TargetType="Border">
            <Setter Property="Background" Value="White"/>
            <Setter Property="BorderBrush" Value="#E0E0E0"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="CornerRadius" Value="6"/>
            <Setter Property="Padding" Value="12"/>
            <Setter Property="Margin" Value="0,0,0,12"/>
        </Style>
        
        <!-- Badge style -->
        <Style x:Key="Badge" TargetType="Border">
            <Setter Property="CornerRadius" Value="4"/>
            <Setter Property="Padding" Value="8,3"/>
            <Setter Property="Margin" Value="0,0,6,0"/>
        </Style>
        
        <!-- Metadata row style -->
        <Style x:Key="MetadataLabel" TargetType="TextBlock">
            <Setter Property="FontSize" Value="11"/>
            <Setter Property="Foreground" Value="#888"/>
            <Setter Property="MinWidth" Value="60"/>
            <Setter Property="Margin" Value="0,0,8,0"/>
        </Style>
        
        <Style x:Key="MetadataValue" TargetType="TextBlock">
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Foreground" Value="#333"/>
            <Setter Property="TextWrapping" Value="NoWrap"/>
            <Setter Property="TextTrimming" Value="CharacterEllipsis"/>
        </Style>
        
        <BooleanToVisibilityConverter x:Key="BoolToVis"/>
    </Window.Resources>
    
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <!-- Header -->
        <Border Grid.Row="0" Background="White" BorderBrush="#E0E0E0" BorderThickness="0,0,0,1" Padding="16,12">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                
                <!-- Icon -->
                <Border Grid.Column="0" Width="42" Height="42" CornerRadius="8" 
                        Background="{Binding IconBackground}" Margin="0,0,14,0">
                    <emoji:TextBlock Text="{Binding Icon}" FontSize="22" 
                               HorizontalAlignment="Center" VerticalAlignment="Center"/>
                </Border>
                
                <!-- Title and subtitle -->
                <StackPanel Grid.Column="1" VerticalAlignment="Center">
                    <TextBlock Text="{Binding DisplayTitle}" 
                               FontSize="16" FontWeight="SemiBold" Foreground="#1A1A1A"
                               TextTrimming="CharacterEllipsis"/>
                    <TextBlock Text="{Binding Subtitle}" 
                               FontSize="11" Foreground="#888" Margin="0,2,0,0"
                               Visibility="{Binding HasSubtitle, Converter={StaticResource BoolToVis}}"/>
                </StackPanel>
                
                <!-- Badges -->
                <ItemsControl Grid.Column="2" ItemsSource="{Binding Badges}" VerticalAlignment="Center">
                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                            <StackPanel Orientation="Horizontal"/>
                        </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Border Style="{StaticResource Badge}" Background="{Binding Background}">
                                <TextBlock Text="{Binding Text}" FontSize="11" Foreground="{Binding Foreground}"/>
                            </Border>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </Grid>
        </Border>
        
        <!-- Metadata Section (optional) -->
        <Border Grid.Row="1" Background="#FAFBFC" Padding="16,10" BorderBrush="#E0E0E0" BorderThickness="0,0,0,1"
                Visibility="{Binding HasMetadata, Converter={StaticResource BoolToVis}}">
            <ItemsControl ItemsSource="{Binding MetadataItems}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Grid Margin="0,2">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            
                            <emoji:TextBlock Grid.Column="0" Text="{Binding Icon}" FontSize="12" 
                                       VerticalAlignment="Center" Margin="0,0,8,0"/>
                            <TextBlock Grid.Column="1" Text="{Binding Value}" 
                                       Style="{StaticResource MetadataValue}"
                                       VerticalAlignment="Center"
                                       ToolTip="{Binding Value}"/>
                            <Button Grid.Column="2" 
                                    Content="{Binding ActionText}"
                                    Visibility="{Binding HasAction, Converter={StaticResource BoolToVis}}"
                                    Click="OnMetadataAction_Click"
                                    Tag="{Binding}"
                                    Background="#F0F0F0" Foreground="#333"
                                    BorderThickness="0" Padding="8,3" Cursor="Hand"
                                    FontSize="10" Margin="8,0,0,0">
                                <Button.Template>
                                    <ControlTemplate TargetType="Button">
                                        <Border x:Name="border" Background="{TemplateBinding Background}" 
                                                CornerRadius="3" Padding="{TemplateBinding Padding}">
                                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                        </Border>
                                        <ControlTemplate.Triggers>
                                            <Trigger Property="IsMouseOver" Value="True">
                                                <Setter TargetName="border" Property="Background" Value="#E0E0E0"/>
                                            </Trigger>
                                        </ControlTemplate.Triggers>
                                    </ControlTemplate>
                                </Button.Template>
                            </Button>
                        </Grid>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </Border>
        
        <!-- Content Area -->
        <Border Grid.Row="2" Margin="16,12,16,0" Style="{StaticResource InfoCard}" Padding="0">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                
                <!-- Content Header -->
                <Border Grid.Row="0" Background="#FAFAFA" BorderBrush="#E0E0E0" BorderThickness="0,0,0,1" Padding="12,8">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        
                        <StackPanel Grid.Column="0" Orientation="Horizontal">
                            <emoji:TextBlock Text="{Binding ContentIcon}" FontSize="12" Margin="0,0,6,0" VerticalAlignment="Center"/>
                            <TextBlock Text="{Binding ContentTypeLabel}" FontSize="12" FontWeight="Medium" Foreground="#333" VerticalAlignment="Center"/>
                            <TextBlock Text="{Binding ContentStats}" FontSize="11" Foreground="#888" Margin="8,0,0,0" VerticalAlignment="Center"/>
                        </StackPanel>
                        
                        <!-- Copy button -->
                        <Button Grid.Column="1" 
                                Click="OnCopyContent_Click"
                                ToolTip="Copy to clipboard"
                                Background="Transparent" BorderThickness="0" 
                                Padding="6,3" Cursor="Hand">
                            <Button.Template>
                                <ControlTemplate TargetType="Button">
                                    <Border x:Name="border" Background="{TemplateBinding Background}" 
                                            CornerRadius="3" Padding="{TemplateBinding Padding}">
                                        <StackPanel Orientation="Horizontal">
                                            <emoji:TextBlock Text="📋" FontSize="12" Margin="0,0,4,0"/>
                                            <TextBlock Text="Copy" FontSize="10" Foreground="#666" VerticalAlignment="Center"/>
                                        </StackPanel>
                                    </Border>
                                    <ControlTemplate.Triggers>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter TargetName="border" Property="Background" Value="#F0F0F0"/>
                                        </Trigger>
                                    </ControlTemplate.Triggers>
                                </ControlTemplate>
                            </Button.Template>
                        </Button>
                    </Grid>
                </Border>
                
                <!-- Scrollable Content -->
                <ScrollViewer Grid.Row="1" 
                              VerticalScrollBarVisibility="Auto" 
                              HorizontalScrollBarVisibility="Auto">
                    <TextBox x:Name="ContentTextBox"
                             Text="{Binding Content, Mode=OneWay}"
                             IsReadOnly="True"
                             Background="Transparent"
                             BorderThickness="0"
                             FontFamily="{Binding ContentFontFamily}"
                             FontSize="{Binding ContentFontSize}"
                             Foreground="#333"
                             TextWrapping="{Binding TextWrapping}"
                             AcceptsReturn="True"
                             Padding="12"
                             VerticalScrollBarVisibility="Disabled"
                             HorizontalScrollBarVisibility="Disabled"/>
                </ScrollViewer>
            </Grid>
        </Border>
        
        <!-- Footer -->
        <Border Grid.Row="3" Background="#FAFAFA" BorderBrush="#E0E0E0" BorderThickness="0,1,0,0" Padding="16,12">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                
                <!-- Optional footer text -->
                <TextBlock Grid.Column="0"
                           Text="{Binding FooterText}" 
                           FontSize="11" Foreground="#666" 
                           TextWrapping="Wrap"
                           VerticalAlignment="Center"
                           Visibility="{Binding HasFooterText, Converter={StaticResource BoolToVis}}"/>
                
                <Button Grid.Column="1" Content="Close" 
                        Click="OnClose_Click"
                        IsDefault="True"
                        Background="#0078D4" Foreground="White"
                        BorderThickness="0" Padding="20,8" Cursor="Hand"
                        FontSize="12" FontWeight="SemiBold">
                    <Button.Template>
                        <ControlTemplate TargetType="Button">
                            <Border x:Name="border" Background="{TemplateBinding Background}" 
                                    CornerRadius="6" Padding="{TemplateBinding Padding}">
                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Border>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter TargetName="border" Property="Background" Value="#1084D8"/>
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Button.Template>
                </Button>
            </Grid>
        </Border>
    </Grid>
</Window>